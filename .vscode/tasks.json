{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build C (Debug)",
            "type": "shell",
            "command": "clear ; rm ${workspaceFolder}/bin/* ; gcc main.c -ggdb -o ${workspaceFolder}/bin/tp_integrador",
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "new",
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}